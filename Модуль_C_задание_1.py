```markdown
# README.md

## Модуль «Курьер»

---

### 1. Код из выдачного материала

```python
energy = 10
while True:
    cmd = input("Команда: ")
    if cmd == "стоп":
        print("Итог:", energy)
        break
    elif cmd == "шаг":
        energy = _________________
        if energy <= 0:
            print("Сел")
            break
        print("Заряд:", energy)
    elif cmd == "заряд":
        energy = energy + 3
        if energy > 10:
            energy = 10
        print("Заряд:", _________________)
    else:
        print(_________________)
```

---

### 2. Отчёт по ревью

**ФИО:** Кондря Владислав  
**Группа:** 35IS  
**Вариант:** Модуль «Курьер»

---

#### 1. Что требует ТЗ

Робот возит посылки. Заряд 10. Команды: шаг, заряд, стоп.

Правила:
- `шаг` — заряд минус 2, печать «Заряд: ...»;
- `заряд` — плюс 3, но не выше 10, печать «Заряд: ...»;
- `стоп` — печать «Итог: ...» и выход;
- другая команда — печать «Не понял»;
- если после шага заряд 0 или меньше — печать «Сел» и выход. Итог не печатать.

#### 2. Что код делает на самом деле

Код представляет собой шаблон с пропусками. В местах, где стоят прочерки, код не работает — синтаксическая ошибка.

#### 3. Несоответствия

1. В строке `energy = _________________` пропущено выражение `energy - 2`.
2. В строке `print("Заряд:", _________________)` пропущено `energy`.
3. В строке `print(_________________)` пропущена строка `"Не понял"`.

#### 4. Вердикт

Отклонить

#### 5. Что исправить

Заполнить пропуски:
- `energy = energy - 2`
- `print("Заряд:", energy)`
- `print("Не понял")`

---

### 3. Исправленный код

```python
energy = 10
while True:
    cmd = input("Команда: ")
    if cmd == "стоп":
        print("Итог:", energy)
        break
    elif cmd == "шаг":
        energy = energy - 2
        if energy <= 0:
            print("Сел")
            break
        print("Заряд:", energy)
    elif cmd == "заряд":
        energy = energy + 3
        if energy > 10:
            energy = 10
        print("Заряд:", energy)
    else:
        print("Не понял")
```

---

### 4. Проверка примеров (ревью)

| Проверка | да/нет | Правка одной строкой, если нет |
|----------|--------|--------------------------------|
| шаг, шаг, стоп → Итог: 6 | да | — |
| шаг, заряд, стоп → Итог: 10 | да | — |
| прыг, стоп → Не понял, Итог: 10 | да | — |
| 5 шагов → Сел | да | — |

---

### 5. Роли

| Роль | ФИО |
|------|-----|
| Программист | Кондря Владислав |
| Ревьюер | Кондря Владислав |
```




