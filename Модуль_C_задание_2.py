```markdown
# README.md

## Модуль «Сейф»

---

### 1. Код из выдачного материала

```python
secret = "421"
tries = 3
while tries > 0:
    code = input("Код: ")
    if code == secret:
        print(_________________)
        break
    tries = _________________
    if tries == 0:
        print("Закрыт")
    else:
        print("Осталось:", tries)
```

---

### 2. Отчёт по ревью

**ФИО:** Кондря Владислав  
**Группа:** 35IS  
**Вариант:** Модуль «Сейф»

---

#### 1. Что требует ТЗ

Программа:
- секрет в переменной `secret` равен `"421"`;
- попыток сначала 3;
- пока попытки больше 0 — спрашивают код;
- если ввод совпал с секретом — печатают «Открыт» и выходят;
- если не совпал — попыток становится на одну меньше;
- если после этого попыток 0 — печатают «Закрыт»;
- если попытки ещё есть — печатают «Осталось:» и число;
- код сравнивают как строку, не превращать в int.

#### 2. Что код делает на самом деле

Код представляет собой шаблон с двумя пропусками. Без их заполнения программа не работает.

#### 3. Несоответствия

1. В строке `print(_________________)` пропущено `"Открыт"`.
2. В строке `tries = _________________` пропущено `tries - 1`.

#### 4. Вердикт

Отклонить

#### 5. Что исправить

Заполнить пропуски:
- `print("Открыт")`
- `tries = tries - 1`

---

### 3. Исправленный код

```python
secret = "421"
tries = 3
while tries > 0:
    code = input("Код: ")
    if code == secret:
        print("Открыт")
        break
    tries = tries - 1
    if tries == 0:
        print("Закрыт")
    else:
        print("Осталось:", tries)
```

---

### 4. Проверка примеров (ревью)

| Проверка | да/нет | Правка одной строкой, если нет |
|----------|--------|--------------------------------|
| 421 → Открыт | да | — |
| 111, 421 → Осталось: 2, Открыт | да | — |
| 000, 111, 222 → Закрыт | да | — |

---

### 5. Роли

| Роль | ФИО |
|------|-----|
| Программист | Кондря Владислав |
| Ревьюер | Кондря Владислав |
```