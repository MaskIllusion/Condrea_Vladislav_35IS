```markdown
# README.md

## Вариант 8. Билеты

---

### 1. Код из выдачного материала

```python
def ticket(age):
    # --- дальше по ТЗ, строки 05-09 ---
    kind = ""
    return kind

try:
    age = int(input("Возраст: "))
    print(ticket(age))
except ValueError:
    print("Нужно целое число")
```

---

### 2. Отчёт по ревью

**ФИО:** Кондря Владислав  
**Группа:** 35IS  
**Вариант:** 8

---

#### 1. Что требует ТЗ

Программа:
- спрашивает возраст;
- если ввели не целое число — печатает «Нужно целое число» и заканчивает;
- функция ticket(age) возвращает:
  - если возраст меньше 0 или больше 120 — «Возраст не подходит»;
  - если возраст от 0 до 6 включительно — «Бесплатно»;
  - если возраст от 7 до 17 включительно — «Детский билет»;
  - если возраст от 18 до 64 включительно — «Обычный билет»;
  - иначе — «Льготный билет».

#### 2. Что код делает на самом деле

Код представляет собой заготовку с пустой функцией ticket(), которая всегда возвращает пустую строку. Основная программа запрашивает возраст, но не выводит корректную категорию билета.

#### 3. Несоответствия

1. Функция ticket() не реализована — всегда возвращает пустую строку.
2. Нет проверки условий из ТЗ (границы возрастов).
3. Программа не выводит нужный текст, только пустую строку.

#### 4. Вердикт

Отклонить

#### 5. Что исправить

Реализовать функцию ticket() по алгоритму из ТЗ:
- проверить age < 0 или age > 120 → "Возраст не подходит";
- проверить 0 <= age <= 6 → "Бесплатно";
- проверить 7 <= age <= 17 → "Детский билет";
- проверить 18 <= age <= 64 → "Обычный билет";
- иначе → "Льготный билет".

---

### 3. Проверка примеров (ревью)

| Проверка | да/нет | Правка одной строкой, если нет |
|----------|--------|--------------------------------|
| 6 → Бесплатно | да | — |
| 7 → Детский билет | да | — |
| 18 → Обычный билет | да | — |
| 65 → Льготный билет | да | — |
| -1 и 121 → Возраст не подходит | да | — |
| абв не роняет программу | да | — |
| Есть функция ticket | да | — |

---

### 4. Исправленный код

```python
def ticket(age):
    if age < 0 or age > 120:
        kind = "Возраст не подходит"
    elif 0 <= age <= 6:
        kind = "Бесплатно"
    elif 7 <= age <= 17:
        kind = "Детский билет"
    elif 18 <= age <= 64:
        kind = "Обычный билет"
    else:
        kind = "Льготный билет"
    return kind

try:
    age = int(input("Возраст: "))
    print(ticket(age))
except ValueError:
    print("Нужно целое число")
```

---

### 5. Роли

| Роль | ФИО |
|------|-----|
| Программист | Кондря Владислав |
| Ревьюер | Кондря Владислав |
```